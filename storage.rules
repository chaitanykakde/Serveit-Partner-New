rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY: Allow all read and write operations without restrictions
    // TODO: Update with proper security rules after release
    match /{allPaths=**} {
      allow read, write, delete: if true;
    }
  }
}

